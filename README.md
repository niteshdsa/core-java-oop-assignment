Assignment Title: Student Product Billing & Management System 

Name:   Nitesh Kumar 

Date:   13/04/2026

Concept Used:   1. Class & Object; 
                   e.g.: Student s = new Student(); 
                2. Constructor; 
                   e.g.: Employee(int id, String name, double salary)
                3. Encapsulation; 
                   e.g.: private int id;
                   void setProduct(...)  // setter
                   void getProduct()     // getter
                4. Method Overloading; 
                   e.g.: void bill(int a)
                         void bill(int a, int b)
                         void bill(int a, int b, int c)
                5. Inheritance; 
                   e.g.: class CollegeStudent extends StudentBase
                6. Method Overriding; 
                   e.g.: void showDetails() {
                         super.showDetails();
                         }
                7. super Keyword; 
                   e.g.: super(name, comments);
                8. this Keyword; 
                   e.g.: this.id = id;
                9. Array; 
                   e.g.: int marks[] = {70, 80, 90};
                10. Loop (for loop); 
                    e.g.: for(int i=0; i<marks.length; i++)
                11. String Methods; 
                    e.g.: toUpperCase(), toLowerCase(), length()
                12. Static Variable; 
                    e.g.: static String companyName;
                13. Access Modifiers; 
                    e.g.: private → data hiding
                          public → accessible everywhere

How to Run:   1. Compile the Program; 
               Open terminal in project folder and run:
               javac Main.java
              2. Run the Program; 
               java Main
                          
Sample Output:   Student details; 
                 Name formatting; 
                 Marks array; 
                 Employee details; 
                 Product details; 
                 Billing (method overloading); 
                 Inheritance example;                          


